let foo = 50 / 2;

const obj = {
    x: 100,
    y: 32,
    foo: foo,
    complex: {
        bar: true,
    },
};

func add(x, y) {
    let result = x + y;
    result + 5
}

const result = add(10, 4);

print(result)